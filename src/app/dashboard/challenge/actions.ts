'use client'

import { createClient } from "@/utils/supabase/client";


export function postChallenge(formData: FormData){
    console.log(formData);
    const supabase = createClient(); 

    

    
}